<UserControl x:Class="MusicSteak.Views.PropertiesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Core.ViewModels;assembly=Core"
        xmlns:views="clr-namespace:MusicSteak.Views"
        xmlns:c="clr-namespace:MusicSteak.Converters"
        d:DataContext="{d:DesignInstance Type=vm:PropertiesViewModel, IsDesignTimeCreatable=False}"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <views:ToolView />
        <ListView
            Grid.Row="1">
        </ListView>
    </Grid>
    
</UserControl>
